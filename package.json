{
  "name": "ow-tag-tester",
  "version": "1.0.0",
  "description": "A web application to validate ad tags against defined rules.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "build": "npm install && cd server && npm install && cd ../client && npm install --production=false && npm run build && echo 'Client Build Contents:' && ls -la && ls -la dist",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "body-parser": "^2.2.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pdf-parse": "^2.4.5"
  }
}